# Legacy Python implementation

This directory contains the retired Python implementation of `disk-agent`.
It is preserved for historical reference only.

The active implementation is the Rust crate at the repository root. Do not use
this Python package for normal operation, packaging, or new feature work.
